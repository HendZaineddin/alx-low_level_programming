#include "main.h"
/**
 * _strlen - a function that returns the length of a string.
 * @s: the string.
 * Return: R.
 */

int _strlen(char *s)
{
	int R = 0;

	for (R = 0; s[R] != '\0' ; R++)
	{
	}
return (R);
}
