#include "main.h"
/**
 *
 *
 */

void print_rev(char *s)
{
	   int i, g;

   for (i = 0; s[i] = '\0'; i++)
   
       g = i - 1;
       for (g = i - 1; s[g] = '\0'; g--)
       {
           putchar(s[g]);
       }
        putchar('\n');
}
