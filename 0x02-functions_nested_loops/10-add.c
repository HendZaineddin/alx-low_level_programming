#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @M: first integer.
 * @N: second interger.
 * Return: Add.
 */

int add(int M, int N)
{
	int Add = M + N;

	return (Add);
}
