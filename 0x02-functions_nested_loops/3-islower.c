#include "main.h"
/**
 * _islower - function check lowercase.
 * @c: checks input.
 * Return: return 1 if 'c' is lowercase otherwise always 0.
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
