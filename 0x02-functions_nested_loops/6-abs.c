#include "main.h"
/**
 * _abs - Function to find the absolute value of an integer.
 * @k: computes the absolute value of an integer.
 * Return: 0.
 */

int _abs(int k)
{

	if (k >= 0)
	{
		return (0);
	}
	else
		return (-1);
}

