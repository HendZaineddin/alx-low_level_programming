#include "main.h"
/**
 * _abs - Function to find the absolute value of an integer.
 * @N: computes the absolute value of an integer.
 * Return: 0.
 */

int _abs(int N)
{

	if (N < 0)
			N = (-1) * N;
		return (0);
}
