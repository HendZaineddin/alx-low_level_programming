#include "main.h"
/**
 * _abs - Function to find the absolute value of an integer.
 * @l: computes the absolute value of an integer.
 * Return: 0.
 */

int _abs(int l)
{

	if (l < 0)
			l = (-1) * l;
		return (0);
}
